import { GameBoard } from "./gameBoard";
import { Ship } from "./ship";

// describe("Legal moves", () => {
//   test("(0,1) should not be in legal moves", () => {
//     expect(player.legalMoves).toEqual(expect.arrayContaining([0, 1])); // add not to
//   });
//   test("(0,0) should be in legal moves", () => {
//     expect(player.legalMoves).toEqual(expect.arrayContaining([0, 0]));
//   });
// });
